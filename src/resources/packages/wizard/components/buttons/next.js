import { Button } from '@wordpress/components';

const handleNext = () => {};

const NextButton = () => (
	<Button
	  variant="primary"
	  onClick={ handleNext }
	>
	  Continue
	</Button>
);

export default NextButton;
