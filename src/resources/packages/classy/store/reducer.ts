import { Action } from '../../../../../common/src/resources/packages/classy/types/Actions';
import { StoreState } from '../types/StoreState';

export const reducer = ( state: StoreState, action: Action ) => {
	return state;
};
