/* -----------------------------------------------------------------------------
 *
 * Day Marker
 *
 * ----------------------------------------------------------------------------- */

.tribe-events {

	.tribe-events-c-day-marker {

		&:after {
			background-color: var(--color-border-default);
			content: '';
			display: block;
			flex: auto;
			height: 1px;
			margin-left: 20px;
		}
	}
}
