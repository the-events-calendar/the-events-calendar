.tribe-events {

	/* -----------------------------------------------------------------------------
	 * Nav
	 * ----------------------------------------------------------------------------- */

	.tribe-events-calendar-list-nav {
		padding-top: var(--spacer-5);

		@media (--viewport-medium) {
			padding-top: var(--spacer-7);
		}
	}
}
