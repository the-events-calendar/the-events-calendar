/* -----------------------------------------------------------------------------
* Event Status.
* ----------------------------------------------------------------------------- */
.tribe-events {

	.tribe-events-status-single__header {
		color: var(--tec-color-text-primary);
	}

	.tribe-events-status-single__header {
		color: var(--tec-color-icon-error);
	}

	.tribe-events-status-single__header--alert-icon:before {
		background-image: url('../images/alert.svg');
		background-repeat: no-repeat;
		background-size: var(--tec-spacer-3);
		content: '';
		display: inline-block;
		height: var(--tec-spacer-3);
		margin-right: var(--tec-spacer-1);
		min-width: var(--tec-spacer-3);
	}

	/* -----------------------------------------------------------------------------
	 * Event Status (Canceled, Postponed)
	 * ----------------------------------------------------------------------------- */

	.tribe-events-status-label__text {
		color: var(--tec-color-icon-error);
	}

	/* -----------------------------------------------------------------------------
	* Single.
	* ----------------------------------------------------------------------------- */

	.tribe-events-status-single {
		border: 1px solid var(--tec-color-border-default);
	}

	.tribe-events-status-single__notice-postponed,
	.tribe-events-status-single__notice-canceled {
		border-left-color: var(--tec-color-icon-error);
	}
}
