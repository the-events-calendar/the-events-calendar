Significance: minor
Type: feat

Added the "All" Views v2 view rendering the archive of all the Occurrences of an Event at `/event/{slug}/all/` when Events Calendar Pro is not active: a list-style view scoped to the Event, with breadcrumbs, a dedicated title, upcoming and past modes and page-based pagination.
