actor: Blocks_editor_integrationTester
modules:
    enabled:
        - \Helper\Blocks_editor_integration
        - WPLoader
    config:
        WPLoader:
            wpRootFolder: "%WP_ROOT_FOLDER%"
            dbUrl: 'mysql://%WP_TEST_DB_USER%:%WP_TEST_DB_PASSWORD%@%WP_TEST_DB_HOST%/%WP_TEST_DB_NAME%'
            tablePrefix: test_
            domain: "%WP_DOMAIN%"
            adminEmail: admin@tribe.localhost
            title: 'The Events Calendar Tests'
            plugins:
                - the-events-calendar/the-events-calendar.php
