name: 'PHP CodeSniffer'
on:
  pull_request:
    paths:
      - 'src/**.php'
      - '*.php'
      - '.github/workflows/phpcs.yml'
jobs:
  phpcs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      # ------------------------------------------------------------------------------
      # Prepare our cache directories
      # ------------------------------------------------------------------------------
      - name: Get Composer Cache Directory
        if: steps.s3_zip.outcome != 'success'
        id: get-composer-cache-dir
        run: |
          echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT
      - uses: actions/cache@v2
        if: steps.s3_zip.outcome != 'success'
        id: composer-cache
        with:
          path: ${{ steps.get-composer-cache-dir.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - uses: "shivammathur/setup-php@v2"
        with:
          php-version: "latest"
      - uses: "ramsey/composer-install@v2"

      - name: "Give permissions"
        run: |
          sudo chown -R root:root $GITHUB_WORKSPACE

      # ------------------------------------------------------------------------------
      # PHPCS
      # ------------------------------------------------------------------------------
      - name: phpcs
        uses: shirobrak/action-phpcs@v1.0.0
        with:
          github_token: ${{ secrets.GH_BOT_TOKEN }}
          reporter: github-pr-review  # Change reporter.
          phpcs_flags: '--runtime-set installed_paths ../../automattic/vipwpcs,../../sirbrillig/phpcs-variable-analysis,../../the-events-calendar/coding-standards,../../wp-coding-standards/wpcs,vendor/wp-coding-standards/wpcs,vendor/the-events-calendar/coding-standards --standard=phpcs.xml src/'