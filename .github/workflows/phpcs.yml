name: 'Linting'
on:
  pull_request:
    paths:
      - '*.php'
      - 'src/**.php'
jobs:
  phpcs:
    uses: stellarwp/github-actions/.github/workflows/phpcs.yml@main
    with:
      ref: ${{ github.event.inputs.ref }}
    secrets: inherit